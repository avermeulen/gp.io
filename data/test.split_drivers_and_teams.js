// open the file

//read each line and check the length

//shorter than 9 is manuf

//the manuf after the driver is this allocated to the driver

var assert = require("assert");

describe("Split Drivers from Teams", function () {
	it("should find all the teams", function () {
		
		assert.ok(1 === 2);

	});

	it("should find all the drivers", function () {
		// body...
	});
})